name: Testing-env-in-github-actions
on:
  workflow_dispatch:
env:
  workflow-name: Workflow-Mayur
jobs:
 Build:
  runs-on: ubuntu-24.04
  env:
   job-name: Job-Mayur
  steps:
   - name: Testing Env variable
     env:
      step-name: Step-Mayur
     run: echo " Testing Env"
     run: echo "{env.workflow-name}"
     run: echo "{env.job-name}"
     run: echo "{env.step-name}"
