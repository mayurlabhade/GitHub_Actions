name: Steps-Types
on:  
    workflow_dispatch:
jobs:
  job-1:
  runs-on: ubuntu-24.04
  steps:
    - name: step-1
      run: echo " Hello From ubuntu"
    - name: step-2
      run: print("Hello From Python")
      shell: python
    
     
